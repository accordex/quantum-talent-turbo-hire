# TalentTurbo — Reference Backend

This is a minimal Node.js + Express scaffold that implements all the API
endpoints the frontend expects. It uses an in-memory store by default —
swap it out for a real database (Postgres, MySQL, MongoDB, etc.) as needed.

## Run

```bash
cd server
npm install
node index.js
```

Server starts on http://localhost:4000