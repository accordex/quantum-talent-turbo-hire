name: Deploy Dev Branch

on:
  push:
    branches:
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to Server
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ vars.SERVER_HOST }}
          username: ${{ vars.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          debug: true
          script: |
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
            cd /var/www/quantumtalentturbohire.softmerce.com/quantum-talent-turbo-hire
            git pull origin dev
            npm install
            npm run migrate
            npm run build
            pm2 restart quantum-talent-turbo-hire || pm2 start npm --name "quantum-talent-turbo-hire" -- start
